---
- hosts: all
  roles:
   - role: users-add
     vars:
       ansible_user: root
   - preconfig-all

# 02 Сделать преконфиг для хостов manager
- hosts: manager
  roles:
    - preconfig-master
    - docker-registry-install
    - swarm_init

- hosts: worker
  roles:
    - role: swarm_join
      type: worker
